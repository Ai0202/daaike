import SectionTitle from '@/components/atoms/SectionTitle'

const Works: React.FC = () => {
  return (
    <section className="w-full mt-8 px-8 text-neutral-text">
      <SectionTitle title="Works" />
      <section>セクションbody</section>
    </section>
  )
}

export default Works
